dependencies:
  override:
    - pip install tox tox-pyenv
    - pyenv local 2.7.13 3.4.6 3.5.3 3.6.1
  post:
    - pip install coverage
    - python ./setup.py install
test:
  post:
    - coverage run --source eventtools ./runtests.py
    - bash <(curl -s https://codecov.io/bash)
    
    
